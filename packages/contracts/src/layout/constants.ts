export const CHROME_TOP_OFFSET = 10;
export const CHROME_BAR_HEIGHT = 48;
/**
 * Safe chrome height offset for WebContentsView bounds.
 * Total height = top offset (10px) + titlebar height (48px) + safe margin (6px) = 64px.
 */
export const CHROME_HEIGHT = CHROME_TOP_OFFSET + CHROME_BAR_HEIGHT + 6;

export const WINDOW_CONTROLS_WIDTH = 110;
export const TAB_RAIL_WIDTH = 176;
